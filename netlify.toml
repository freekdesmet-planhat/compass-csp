[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes served by the client router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
